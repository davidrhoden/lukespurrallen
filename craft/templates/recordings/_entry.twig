{#
 # News entry template
 # -------------------
 #
 # See this page for more details on how Craft routes requests:
 # http://craftcms.com/docs/routing
 #}

{% extends "_layout" %}

{% block content %}
	<article>
		{% for asset in entry.albumArt %}
			<img src="{{ asset.getUrl('threeHundredTwentyPxWide') }}" />
		{% endfor %}
		<h1>{{ entry.title }}</h1>
		<p>{{ entry.body }}</p>
		<p>{{ entry.downloadLink }}</p>
	</article>
{% endblock %}
